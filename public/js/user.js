export var NODE = 1;
export var nodepath = '/node' + NODE;
export var uncommittedMovies = [];

/**
 * Removes movies that have been successfully committed
 * @param {Array} arr array of ids to remove
 */
export function removeCommittedMovies(arr){

}